# Local model folder

Place one or more `.gguf` chat or instruction models here. LocalChat scans this folder and its subfolders when you choose **Refresh** in **Model & settings**.

Model files are intentionally ignored by Git because they are large and may have separate licenses. Verify a model's source, license, memory requirements, and chat-template support before using it.
